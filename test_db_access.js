const tables = ['profile', 'match', 'activity', 'match_round', 'activity_participant']; async function checkTables() { try { const { supabase } = await import('./src/integrations/supabase/client.js'); for (const table of tables) { console.log(); const { data, error } = await supabase.from(table).select('count(*)', { count: 'exact', head: true }); if (error) { console.error(, error); } else { console.log(, data); } } } catch (e) { console.error('Script error:', e); } } checkTables();
